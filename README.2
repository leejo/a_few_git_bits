
Create aliases and ignore them in your history

	st = status -sb
	ci = commit
	co = checkout
	br = !git branch -vv
	rso = remote show origin

You can even add project specific aliases (e.g. config)

	https://github.com/dbsrgits/dbix-class/commit/1f99022a
