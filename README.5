
git rebase

    * slow motion action replay your commits

    * re-order commits

    * reword commit messages downstream

    * squash commits downstream
        * useful if you're doing lots of small WIP commits

    * re-work commits downstream
        * useful if you find a commit has broken tests

    * don't rebase publicly shared pushed out work!
