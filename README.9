
git-dir-blame

    * git doesn't track directories, but we can fake it

    * just a wrapper around git log

    * useful for a quick "how long since files in $dir were modified"
        * find dead code, maybe?
